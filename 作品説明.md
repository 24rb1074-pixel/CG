# 3D Tetris ― 作品説明・評価基準対応表

## 1. 作品概要

Python、GLFW、PyOpenGL、NumPy、Pillowを用いて制作した3Dテトリスである。10×20マスのフィールド上で7種類のテトリミノを操作し、横一列を揃えて消去する。通常の移動・回転・落下に加え、NEXT 5個表示、HOLD、ゴースト、ハードドロップ、ロックディレイ、レベル進行を実装した。ルールは2Dテトリスを基礎としているが、すべてのブロック、フィールド、床、背景、スコアを3D空間内に配置している。

## 2. 操作方法

| キー | 操作内容 | プログラム上の処理 |
|---|---|---|
| ←／→ | ミノを左右へ1マス移動 | 移動先の衝突判定後に座標を更新 |
| ↓ | ソフトドロップ | 衝突しない場合に1マス下降 |
| Space | ハードドロップ | 着地するまで下降し、即座に固定 |
| ↑／E | 右回転 | 回転行列相当の座標変換とウォールキック |
| Q／左Ctrl | 左回転 | 右回転と逆向きの座標変換 |
| C／左Shift | ホールド | 現在のミノを保存・交換。1回の落下につき1度だけ使用可能 |
| Escape | 一時停止／再開 | 落下と固定タイマーを停止し、再開時に時間を補正 |
| R | リスタート | スコア、フィールド、キューなどを初期化 |
| U | 終了 | ウィンドウへ終了要求を送る |

ウィンドウタイトルには、スコア、消去ライン数、レベル、`Playing`、`PAUSED`、`GAME OVER`の状態を表示する。

---

## 3. 評価基準との対応

### 3.1 CGの基礎技術が組み込まれているか

#### A. モデリング：オブジェクトは複数で構成されているか

本作品では、単一の図形だけでなく、次の複数オブジェクトから画面を構成している。

| オブジェクト | 構成方法 |
|---|---|
| 操作中のテトリミノ | 4個の立方体 |
| 固定済みブロック | フィールド配列に保存された複数の立方体 |
| ゴースト | 着地点に配置した半透明の立方体4個 |
| NEXT／HOLD | 枠線と、プレビュー用の立方体 |
| スコア | 数字テクスチャを貼った立方体 |
| フィールド | 10×20の格子と外枠 |
| 床・背景 | 四角形プリミティブを並べたチェック模様の平面 |

基本となる立方体は、8個の頂点、6面、12本の辺、6方向の法線ベクトルから自作している。各テトリミノは、I、O、T、S、Z、J、Lごとに4ブロックの相対座標をNumPy配列で保持する。これにより、1種類の立方体描画処理を再利用して複数形状を表現している。

**対応する主な実装：** `vertex`、`edge`、`face`、`normals`、`minos`、`drawCube()`、`drawField()`

#### B. モデリング：階層構造（親子関係）が作られているか

操作中のミノは、ミノ全体の位置を親座標、4個のブロックの相対位置を子座標として扱う。`glPushMatrix()`で変換状態を保存し、`glTranslatef()`でミノ全体を移動してから、各ブロックを局所座標で描画し、最後に`glPopMatrix()`で元の座標系へ戻している。

NEXT表示でも、NEXT領域全体の座標を親、各プレビュー枠を子、各ブロックをさらにその内側の要素として配置している。クラスによるシーングラフではないが、OpenGLの行列スタックを用いた階層変換を実現している。

**対応する主な実装：** `display()`、`drawNextMinoPreview()`、`drawCube()`、`glPushMatrix()`、`glTranslatef()`、`glPopMatrix()`

#### C. モデリング：プリミティブが効果的に使われているか

立方体、四角形、線分という少数のプリミティブを目的別に再利用している。

- `GL_QUADS`：立方体の面、床、背景、テクスチャ面
- `GL_LINES`：立方体の輪郭とフィールド格子
- `GL_LINE_LOOP`：フィールド外枠、NEXT／HOLD枠

立方体の色、透明度、テクスチャ、配置だけを変えることで、操作中ブロック、固定ブロック、ゴースト、プレビュー、スコアという異なる役割を表現している。

#### D. アニメーション：フレームに基づく動きがあるか

メインループでイベント処理、状態更新、描画、バッファ交換を繰り返している。ただし落下速度はPCの描画性能に依存するフレーム数ではなく、`glfw.get_time()`で取得した実時間差から判定する。このため、フレームレートが変わってもゲーム速度が大きく変化しない。

10ライン消去するごとにレベルが1上がり、落下間隔を0.1秒ずつ短縮する。最短間隔は0.1秒に制限している。

**対応する主な実装：** `main()`のメインループ、`display()`、`last_drop_time`、`INITIAL_FALL_INTERVAL`、`MIN_FALL_INTERVAL`

#### E. アニメーション：複数の動きが連動しているか

ミノの位置変更に伴って、次の処理が同じ状態を参照して連動する。

1. 操作中ミノの描画位置が変化する。
2. 衝突判定結果が更新される。
3. ゴーストが新しい着地点へ移動する。
4. 接地するとロックタイマーが開始する。
5. 固定後にライン消去、スコア更新、NEXT繰り上げが行われる。
6. 消去ライン数が一定値を超えると落下速度が変化する。

また、一時停止中は自動落下だけでなくロックタイマーも停止し、再開時に停止時間を加算してタイミングのずれを防いでいる。

#### F. アニメーション：動きの自然さと意図の明確さ

左右移動と回転はマス単位で即時に反応し、落下は一定間隔で進む。滑らかな補間を使わないのは、格子上で正確に操作するテトリスの性質に合わせた設計である。

接地直後に0.5秒のロックディレイを設け、接地後も最大15回まで移動・回転によるタイマーリセットを認めている。これにより、ブロックが触れた瞬間に固定されず、プレイヤーが位置を微調整できる。

#### G. レンダリング：光源・陰影

`GL_LIGHT0`を有効にし、3D空間内の位置光源を設定している。立方体の各面に方向の異なる法線ベクトルを与えているため、同じ色のブロックでも面の向きに応じた明暗が生じ、立体形状を認識できる。

**対応する主な実装：** `LIGHT_POSITION`、`glLightfv()`、`normals`、`glNormal3fv()`、`GL_LIGHTING`

#### H. レンダリング：マテリアル・配色

各ブロックには環境光・拡散反射へ頂点色を反映し、鏡面反射色と光沢度を設定している。7種類のミノにはシアン、黄、マゼンタ、緑、赤、青、オレンジを割り当て、種類を判別しやすくした。さらに黒い輪郭線を重ねることで、同色ブロックが隣接した場合も1マス単位を確認できる。

**対応する主な実装：** `COLORS`、`BLOCK_SPECULAR`、`BLOCK_SHININESS`、`glMaterialfv()`、`glMaterialf()`

#### I. レンダリング：テクスチャ

0～9のPNG画像をPillowで読み込み、上下反転とRGB変換を行った後、OpenGLテクスチャとしてGPUへ転送している。スコアの各桁に対応する数字テクスチャを立方体前面へ貼り付けて表示する。拡大・縮小フィルタには`GL_NEAREST`を使用し、数字の輪郭を明確に保っている。

**対応する主な実装：** `initTextureFromFile()`、`drawScorePreview()`、`digits/0.png`～`digits/9.png`

#### J. レンダリング：透明処理・隠面処理

ゴーストにはアルファブレンディングを使用し、透明度を0.3に設定している。描画中だけ深度バッファへの書き込みを停止し、カリングとライティングの状態を退避・復元することで、他の描画へ設定が漏れないようにした。

通常オブジェクトには深度テストと背面カリングを使用し、手前と奥の位置関係や見えない面を適切に処理している。

**対応する主な実装：** `drawGhostCube()`、`GL_BLEND`、`glBlendFunc()`、`glDepthMask()`、`GL_DEPTH_TEST`、`GL_CULL_FACE`

#### K. レンダリング：カメラと投影

`gluLookAt()`を用い、フィールドの正面右上から中央を見る固定カメラを設定している。透視投影には視野角45度、近クリップ1.0、遠クリップ100.0を使用する。初期ウィンドウは左右のHOLD・NEXTを含む全体が収まりやすい1024×768とした。

ウィンドウサイズ変更時には、フレームバッファの大きさに合わせて`glViewport()`と射影行列を更新する。幅と高さの比からアスペクト比を再計算するため、リサイズ後も縦横比が不自然に引き伸ばされない。

**対応する主な実装：** `CAMERA_EYE`、`CAMERA_CENTER`、`gluLookAt()`、`perspective()`、`resize()`

---

### 3.2 インタラクティブ性

#### A. 操作に対する応答は適切か

キー入力はGLFWのキーボードコールバックで受け取る。移動・下降・回転では、変更後の位置を先に`check_collision()`へ渡し、壁、床、固定ブロックと重ならない場合だけ状態を更新する。このため、不正な位置へ移動してから戻すようなちらつきが発生しない。

ハードドロップは着地位置まで繰り返し衝突判定し、その場で固定する。ゲームオーバー時は通常操作を無効にし、リスタートと終了だけを受け付ける。一時停止中もゲーム操作を無効にする。

#### B. 操作手段の豊富さ

左右移動、ソフトドロップ、ハードドロップ、左右回転、ホールド、一時停止、再開、リスタート、終了に対応する。一部の操作には複数キーを割り当て、好みに合わせて操作できる。

操作を補助する表示として、現在位置から計算したゴースト、5個先までのNEXT、HOLD枠、スコア、ライン数、レベル、ゲーム状態を備えている。マウスには対応していないが、格子上で操作するゲームに適したキーボード操作を一通り実装している。

---

### 3.3 作品としての完成度

#### A. テーマが明確か

テーマは「CG授業で学んだ立体描画技法を、実際に操作できる3Dテトリスへ統合すること」である。すべての主要表示を立方体、平面、線分で統一し、チェック模様の床と背景、照明、輪郭線によって3Dゲームらしい画面を構成した。

#### B. オリジナルな工夫があるか

- 2Dルールを3D立方体と透視投影で表現
- 数字を通常の文字ではなくテクスチャ付き立方体で表示
- ゴーストを半透明の3Dオブジェクトとして表現
- ゲームオーバー時にブロックを灰色化
- NEXTを5個まで3D表示
- 7-bag方式によりミノの極端な偏りを防止
- HOLDを1落下につき1回に制限
- 簡易ウォールキックとロックディレイによる操作性向上
- ライン数に応じたレベル・落下速度の変化

#### C. エラーなく安定して動作するか

GLFW初期化、ウィンドウ生成、数字テクスチャの存在を検査し、失敗時は原因が分かる例外を発生させる。終了処理は`finally`で実行し、途中で例外が発生した場合もテクスチャ、ウィンドウ、GLFWを解放する。

フィールド上端より上にブロックが残ったまま固定される場合や、新しいミノ・HOLD後のミノが出現位置で衝突する場合はゲームオーバーにする。ゼロサイズのウィンドウでは射影行列を更新せず、ゼロ除算を防ぐ。

構文検査に加え、7-bag、衝突判定、ライン消去、レベル計算についてGUIを使用しないロジックテストを実施済みである。

---

### 3.4 説明資料

本資料には、作品概要、操作方法、CG技術、技術的工夫、プログラム構成、安定性、制限事項をまとめた。表を用いて評価基準と実装箇所を対応させ、関数名を記載することでソースコードとの関係を確認しやすくしている。

提出時には本資料をA4横向きまたは複数ページのPDFへ変換し、完成画面のスクリーンショットを「作品概要」付近へ配置すると、画面構成と技術説明の対応がさらに分かりやすくなる。

---

### 3.5 ソースコード

#### A. 提出物どおりに動作するか

実行ファイルは`tetris.py`、スコア用画像は`digits`フォルダ内に配置する。画像パスは`__file__`を基準に組み立てているため、別の作業ディレクトリから実行しても読み込める。

#### B. 可読性（コメント・命名・構造）

ゲーム中に変化する値は`GameState`データクラスへ集約した。フィールド寸法、速度、スコア、ウィンドウ、カメラ、照明、UI配置、色、キー割り当ては用途別の定数としてまとめている。

処理は次のように関数分割している。

| 分類 | 主な関数 |
|---|---|
| 初期化・終了 | `main()`、`init()`、`reset_game()` |
| ゲーム進行 | `display()`、`lock_and_spawn_mino()`、`check_line_clear()` |
| 判定・操作 | `check_collision()`、`rotate_mino()`、`keyboard()` |
| 基本描画 | `drawCube()`、`drawPlaneY()`、`drawPlaneZ()` |
| UI描画 | `drawNextMinoPreview()`、`drawScorePreview()`、`drawPreviewFrame()` |
| 表示設定 | `perspective()`、`resize()`、`update_window_title()` |

コメントは各データと関数の目的を日本語で記述し、複雑な上端判定や描画状態の切り替えには処理理由が分かる説明を付けている。

## 4. 使用データ構造と処理の流れ

フィールドはNumPyの20×10整数配列で管理する。0は空きマス、1～7は各ミノの色IDである。操作中ミノはフィールドへ直接書き込まず、種類、相対座標、基準位置を別に保持する。固定時にだけ4ブロックをフィールドへ記録する。

1フレームでは、現在時刻から自動落下の必要性を判断し、接地状態とロックタイマーを更新する。その後、床、背景、固定ブロック、格子、NEXT、HOLD、ゴースト、操作中ミノ、スコアの順に描画する。

ライン消去では下段から全行を確認し、揃っていない行だけを新しい配列へ下から詰め直す。複数ラインも1回の処理で消去でき、消去数に応じて100、300、500、800点を加算する。

## 5. 既知の仕様・今後の発展

回転時のウォールキックは、左右および上方向へ候補位置をずらす簡易方式であり、公式SRSのミノ別・回転状態別テーブルを完全再現したものではない。スコアは1～4ラインの同時消去を対象とし、T-spin、Back-to-Back、コンボ、ソフト／ハードドロップ距離の加点は採用していない。

今後は、SRSの完全実装、効果音、ライン消去時の補間アニメーション、画面内の文字UI、ランキング保存などを追加することで、さらに完成度を高められる。
